"""Optional domain-specific extensions for RESOLVE.

These modules are not imported by RESOLVE core and must be imported
explicitly by the user (e.g. ``from resolve.ext.wfo import WFOBackbone``).
"""
