# Makevars for resolve R package
# Compile with libtorch and link to C++ core

PKG_CXXFLAGS = $(shell pkg-config --cflags torch) -I../../src/core/include -std=c++17
PKG_LIBS = $(shell pkg-config --libs torch) -L../../src/core/build -lresolve_core

# On Windows, you may need to adjust paths:
# PKG_CXXFLAGS = -I"$(TORCH_DIR)/include" -I"$(TORCH_DIR)/include/torch/csrc/api/include" -I../../src/core/include -std=c++17
# PKG_LIBS = -L"$(TORCH_DIR)/lib" -ltorch -ltorch_cpu -lc10 -L../../src/core/build -lresolve_core
