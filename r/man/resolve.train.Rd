% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve.train}
\alias{resolve.train}
\title{Train a RESOLVE Model}
\usage{
resolve.train(
  dataset,
  speciesEncoding = "hash",
  hiddenDims = NULL,
  maxEpochs = 500L,
  patience = 50L,
  lr = 0.001,
  batchSize = 4096L,
  device = "cpu",
  testSize = 0.2,
  seed = 42L,
  savePath = NULL,
  lossConfig = "mae",
  verbose = TRUE
)
}
\arguments{
\item{dataset}{A resolve.dataset object (from resolve.dataset())}

\item{speciesEncoding}{How to encode species: "hash" (default) or "embed".}

\item{hiddenDims}{Hidden layer dimensions (default c(2048, 1024, 512, 256, 128, 64))}

\item{maxEpochs}{Maximum training epochs (default 500)}

\item{patience}{Early stopping patience (default 50)}

\item{lr}{Learning rate (default 0.001)}

\item{batchSize}{Batch size (default 4096)}

\item{device}{Device: "cpu" or "cuda" (default "cpu")}

\item{testSize}{Fraction of data to use for testing (default 0.2)}

\item{seed}{Random seed (default 42)}

\item{savePath}{Path to save final model (optional)}

\item{lossConfig}{Loss configuration preset: "mae" (default), "smape", or "combined".
\itemize{
\item "mae": Pure MAE loss (no SMAPE, no band penalty)
\item "smape": SMAPE as primary loss from start
\item "combined": Phased training (MAE -> MAE+SMAPE -> MAE+SMAPE+band)
}}

\item{verbose}{Print training progress (default TRUE)}
}
\value{
A trained model object with fit results
}
\description{
Train a model on a dataset with sensible defaults.
}
\examples{
\donttest{
dataset <- resolve.dataset(...)
result <- resolve.train(dataset)
print(result$metrics)
}

}
