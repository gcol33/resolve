% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve.train}
\alias{resolve.train}
\title{Train a RESOLVE Model (legacy facade — removed)}
\usage{
resolve.train(...)
}
\arguments{
\item{...}{Ignored; preserved for argument compatibility with the
pre-removal signature so existing call sites surface a clear error
instead of an "unused argument" message.}
}
\value{
Never returns; always errors.
}
\description{
The legacy \code{resolve.train()} facade trained from a \code{resolve.dataset}
object produced by the removed \code{resolve.dataset()} function, which
depended on the equally removed \code{resolve.encoder()} (see those
functions' help pages for the underlying engine change).
}
\details{
Calling \code{resolve.train()} therefore raises an error. Use
\code{\link[=resolve.train.dataset]{resolve.train.dataset()}} together with \code{\link[=resolve.dataset.csv]{resolve.dataset.csv()}} —
that pair uses the C++ engine end-to-end (no R-side encoding step)
and is the supported training path.
}
