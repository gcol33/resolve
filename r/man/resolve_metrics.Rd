% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{resolve_metrics}
\alias{resolve_metrics}
\alias{resolve_band_accuracy}
\alias{resolve_mae}
\alias{resolve_rmse}
\alias{resolve_smape}
\alias{resolve_accuracy}
\alias{resolve_r_squared}
\title{Prediction metrics}
\usage{
resolve_band_accuracy(pred, target, threshold = 0.25)

resolve_mae(pred, target)

resolve_rmse(pred, target)

resolve_smape(pred, target, eps = 1e-08)

resolve_accuracy(pred, target)

resolve_r_squared(pred, target)
}
\arguments{
\item{pred}{Numeric vector of predicted values (for \code{resolve_accuracy()},
predicted class codes).}

\item{target}{Numeric vector of observed values, the same length as \code{pred}
(for \code{resolve_accuracy()}, the true class codes).}

\item{threshold}{Relative half-width of the band for
\code{resolve_band_accuracy()}: a prediction counts as correct when it lies
within \code{target * (1 - threshold)} and \code{target * (1 + threshold)}.}

\item{eps}{Small constant added to the denominator of the symmetric mean
absolute percentage error to avoid division by zero.}
}
\value{
A single number: the mean absolute error (\code{resolve_mae()}), the
root mean squared error (\code{resolve_rmse()}), the symmetric mean absolute
percentage error in \verb{[0, 2]} (\code{resolve_smape()}), the coefficient of
determination (\code{resolve_r_squared()}), the share of predictions inside
the relative band (\code{resolve_band_accuracy()}), or the share of exactly
matching class codes (\code{resolve_accuracy()}).
}
\description{
The engine's metric functions, computed over a vector of predictions and
the matching vector of observed values. They are the metrics
\code{\link[=resolve.train.dataset]{resolve.train.dataset()}} reports for each target, made available so a
prediction scored elsewhere (a reloaded checkpoint, a held-out survey) can
be evaluated the same way. The \code{resolve_c} backend must be loaded
(\code{\link[=resolve.available]{resolve.available()}}).
}
\examples{
\dontrun{
pred <- c(1.0, 2.2, 2.9, 4.1)
obs <- c(1, 2, 3, 4)
resolve_mae(pred, obs)
resolve_rmse(pred, obs)
resolve_r_squared(pred, obs)
resolve_band_accuracy(pred, obs, threshold = 0.05)
}
}
