% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve.dataset.frame}
\alias{resolve.dataset.frame}
\title{Load a ResolveDataset from in-memory data frames}
\usage{
resolve.dataset.frame(
  header,
  species = NULL,
  roles = list(),
  targets = list(),
  config = list(),
  schemaSource = NULL
)
}
\arguments{
\item{header}{A data.frame of header data (one row per plot). In single-table
mode (\code{species = NULL}) this is instead the long-format species frame,
matching \code{\link{resolve.dataset.csv}}'s single-file behaviour.}

\item{species}{A data.frame of species data (long format), a single file path
string (the large species table is then read once from disk while the
header stays in memory), or \code{NULL} for single-table mode.}

\item{roles}{Named list mapping roles to column names (see
\code{\link{resolve.dataset.csv}}).}

\item{targets}{Named list of target specifications.}

\item{config}{Named list of dataset configuration options.}

\item{schemaSource}{Optional ResolveDataset whose vocabularies / class
mappings are reused (the in-memory analog of \code{schemaSource} in
\code{\link{resolve.dataset.csv}}). Only valid when \code{species} is a
data.frame.}
}
\value{
A ResolveDataset object.
}
\description{
In-memory analog of \code{\link{resolve.dataset.csv}} (issue #22). Builds and
encodes a dataset directly from data frames already in R, eliminating the
write-to-temp-CSV / re-read round-trip that the file-based loader forces when
the header must be filtered or subset before a fit. The result is identical to
\code{\link{resolve.dataset.csv}} on the equivalent CSV; only the disk I/O is
elided.
}
\seealso{
\code{\link{resolve.dataset.csv}}
}
