% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve.load_train_config}
\alias{resolve.load_train_config}
\title{Recover the Training Configuration From a Checkpoint}
\usage{
resolve.load_train_config(path)
}
\arguments{
\item{path}{Path to a \code{.pt} checkpoint written by \code{\link[=resolve.save]{resolve.save()}} or a
training run.}
}
\value{
A named list of training-configuration fields. \code{loss_config} and
\code{lr_scheduler} are integer enum codes.
}
\description{
Reads back the training hyperparameters that were persisted in a RESOLVE
checkpoint (batch size, learning rate, weight decay, phase boundaries,
learning-rate schedule, band thresholds, VRAM fraction, ...) without loading
the model weights. Fields the checkpoint does not persist (device, AMP and
cuDNN flags) come back at their defaults.
}
\examples{
\dontrun{
cfg <- resolve.load_train_config("model.pt")
cfg$batch_size
cfg$band_thresholds
}
}
