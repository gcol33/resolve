% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve.dataset}
\alias{resolve.dataset}
\title{Create a RESOLVE Dataset}
\usage{
resolve.dataset(
  header,
  species,
  roles,
  targets,
  speciesNormalization = "norm",
  trackUnknownFraction = TRUE,
  trackUnknownCount = FALSE,
  hashDim = 32L,
  topK = 5L,
  topKSpecies = 10L,
  selection = "top",
  representation = "abundance"
)
}
\arguments{
\item{header}{Path to plot-level CSV (one row per plot)}

\item{species}{Path to species CSV (one row per species-plot occurrence)}

\item{roles}{Named list mapping semantic roles to column names:
\itemize{
\item plotId: Column with plot IDs in header file
\item speciesId: Column with species names in species file
\item speciesPlotId: Column with plot IDs in species file
\item abundance: Column with abundance values (optional)
\item genus: Column with genus names (optional)
\item family: Column with family names (optional)
\item x: Column with x coordinates (optional)
\item y: Column with y coordinates (optional)
}}

\item{targets}{Named list of target configurations. Each target should have:
\itemize{
\item column: Column name in header file
\item task: "regression" or "classification"
\item transform: "none" or "log1p" (optional)
}}

\item{speciesNormalization}{Normalization mode: "raw", "norm", or "log1p" (default "norm")}

\item{trackUnknownFraction}{Track fraction of unknown species (default TRUE)}

\item{trackUnknownCount}{Track count of unknown species (default FALSE)}

\item{hashDim}{Dimension of species hash embedding (default 32)}

\item{topK}{Number of top genera/families to track (default 5)}

\item{topKSpecies}{Number of top species to track for embed mode (default 10)}

\item{selection}{Selection mode: "top", "bottom", "top_bottom", "all" (default "top")}

\item{representation}{Representation mode: "abundance", "presence_absence" (default "abundance")}
}
\value{
A list containing prepared dataset components:
\itemize{
\item encoder: Fitted SpeciesEncoder
\item coordinates: Matrix of plot coordinates
\item covariates: Matrix of covariate values
\item hashEmbedding: Matrix of species hash embeddings
\item speciesIds: Matrix of top-k species IDs per plot (for embed mode)
\item genusIds: Matrix of genus IDs (if hasTaxonomy)
\item familyIds: Matrix of family IDs (if hasTaxonomy)
\item unknownFraction: Vector of unknown fractions (if trackUnknownFraction)
\item targets: Named list of target vectors
\item schema: Schema information
}
}
\description{
Load and configure a dataset for species composition prediction.
This is a convenience function that reads CSV files and prepares data for training.
}
\examples{
\donttest{
dataset <- resolve.dataset(
  header = "plots.csv",
  species = "species.csv",
  roles = list(
    plotId = "plot_id",
    speciesId = "species",
    speciesPlotId = "plot_id",
    abundance = "cover"
  ),
  targets = list(
    biomass = list(column = "agb", task = "regression")
  )
)
}

}
