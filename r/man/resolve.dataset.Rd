% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve.dataset}
\alias{resolve.dataset}
\title{Create a RESOLVE Dataset (removed)}
\usage{
resolve.dataset(...)
}
\arguments{
\item{...}{Ignored; preserved for argument compatibility with the
pre-removal signature so existing call sites surface a clear error
instead of an "unused argument" message.}
}
\value{
Never returns; always errors.
}
\description{
The legacy \code{resolve.dataset()} facade ran encoding in R via the unified
\code{resolve.encoder()} C++ wrapper, which has been removed (see
\code{\link[=resolve.encoder]{resolve.encoder()}} for the underlying engine change).
}
\details{
Calling \code{resolve.dataset()} therefore raises an error. Use
\code{\link[=resolve.dataset.csv]{resolve.dataset.csv()}} instead — it dispatches to the C++
\code{ResolveDataset::from_csv()} pipeline directly, performs all encoding
inside the engine, and is the input format expected by
\code{\link[=resolve.train.dataset]{resolve.train.dataset()}}.
}
